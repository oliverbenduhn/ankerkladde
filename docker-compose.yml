services:
  einkauf:
    container_name: einkauf
    build: .
    ports:
      - "8083:80"
    volumes:
      - ./data:/data
    restart: unless-stopped
    environment:
      EINKAUF_DATA_DIR: /data
